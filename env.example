# ============================================
# NOTA APP - Environment Variables
# ============================================
# Copy file ini ke .env.local dan isi dengan nilai yang sesuai
# cp env.example .env.local

# ============================================
# Supabase Configuration
# ============================================
# Semua data (barang, jasa, mekanik, transaksi) disimpan di Supabase
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# ============================================
# Catatan Penting
# ============================================
# 1. Jangan commit file .env.local ke git (sudah di .gitignore)
# 2. Untuk production, set environment variables di hosting provider
# 3. Pastikan Supabase project sudah dibuat dan tabel-tabel sudah dibuat
# 4. Buat tabel-tabel yang diperlukan di Supabase Dashboard
